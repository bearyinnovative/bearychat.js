// Generated by ./scripts/generate-api with OpenAPI@1.0.0
// for full api documentation, visit https://github.com/bearyinnovative/OpenAPI
import fetch from 'isomorphic-fetch';
import { getBasePath } from './api';
import { validate } from './_validate';

export default function p2pList(payload) {
  return new Promise((resolve, reject) => {
    const err = validate('/p2p.list', payload);
    if (err) {
      return reject(err);
    }

    const opts = {
      headers: {'Accept': 'application/json'},
      method: 'GET',
    };
    opts.query = payload;

    const api = getBasePath('https://api.bearychat.com/v1') + '/p2p.list';
    return fetch(api, opts);
  });
}